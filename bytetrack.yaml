# ByteTrack Configuration for YOLOv8
# Ultralytics built-in tracker config

# Tracker type
tracker_type: bytetrack

# Detection confidence thresholds
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6

# Track lifecycle
track_buffer: 30
match_thresh: 0.8

# Misc
fuse_score: True
